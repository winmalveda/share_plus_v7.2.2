# melos_managed_dependency_overrides: share_plus,share_plus_platform_interface
dependency_overrides:
  share_plus:
    path: ..
  share_plus_platform_interface:
    path: ../../share_plus_platform_interface
